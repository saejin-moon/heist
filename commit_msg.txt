Comprehensive Codebase Audit, Mathematical Refactor, & Production Optimization

This commit finalizes the rigorously requested 8-Phase codebase audit, resolving theory-to-implementation gaps, dead code bloat, and massive computational bottlenecks.

Mathematical & Theoretical Fixes:
- src/model.py: Fixed missing ROMA KL-Divergence term implementation and MARC/CO-OP Asymmetric Failure Shielding mechanics based on the LaTeX specifications found in paper/.
- src/env.py / src/ppo_utils.py: Implemented zero-division and edge-case padding protections within the simulation resets and the LOO baseline calculations.
- src/train_*.py: Synced disconnected hyperparameter wiring (car_coef, alpha_alarm, max_grad_norm) across CO-OP, CHARM, MAHIRO, ROMA, and LRS trainers.

Dead Code Pruning:
- src/constants.py: Removed abandoned ROLE_IDS, ROLE_ACTIONS, and GLOBAL_STATE_DIM references.
- src/curriculum.py: Stripped out the entirely dead Curriculum python class, preserving only the static config dict.
- src/evaluate.py: Deleted the unused legacy summarize() function.
- tools/status.py: Cleaned dead positional parameters from check_models_status().
- tests/test_curriculum_smoke.py / test_status_smoke.py: Updated dependencies.

Performance & Optimization Upgrades:
- src/model.py (~Line 155): Vectorized MAPPO CIR calculation (HeistAgent.get_influence_matrix) removing serial `for` loops in favor of batched `repeat` tensors.
- src/model.py (~Line 592): Vectorized COMA CIR calculation (ComaAgent.get_influence_matrix) condensing 16 recursive sequential Python calls into 2 batched GPU operations using `gather` and broadcasting.
- Verified all PPO rollouts successfully rely on `torch.no_grad()` to negate graph accumulation.
- Verified QMIX `ReplayBuffer.sample` efficiently exploits `non_blocking=True` memory transfers.

Test Suite Expansion:
- tests/test_model_math.py: Added 9 extensive unit tests focusing on exact dimensional shape constraints, handling of extreme boundary conditions (NaN propagation constraints via PyTorch Categorical), masking bounds, and exact metric outcomes for fundamental CIR matrices.
- Verified 100% test pass status (69/69) executing in 75s.
- Verified `train.zsh` end-to-end production readiness (correctly executing until the deliberate `thermal_guard.py` limit trigger).
